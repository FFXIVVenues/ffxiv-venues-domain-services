info:
  name: Delete Venue
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{scheme}}://{{base-url}}/v1.0/venue/{{?Venue Id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
