info:
  name: Get Venues
  type: http
  seq: 1

http:
  method: GET
  url: "{{scheme}}://{{base-url}}/v1.0/venue"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
